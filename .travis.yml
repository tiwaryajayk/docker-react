sudo: required
services:
  - docker
before_install:
  - docker build -t tiwaryajayk/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=True tiwaryajayk/docker-react npm run test -- --coverage
  